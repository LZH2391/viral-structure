# 后端数据目录与平台能力抽象

本文档已移动到新 UI 后端导航 skill 的 references：

```text
.agents/skills/new-ui-backend-navigator/references/后端数据目录与平台能力抽象.md
```

新 UI 开发时优先使用 `new-ui-backend-navigator` skill，并从该 skill 的 references 读取平台合同和后端索引速查表。

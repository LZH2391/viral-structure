# Assets

可调整资产层。

用于放置高频变化但不属于主流程代码的资产：

- prompt 和模板版本：新增模型调用时，应记录模板名称、版本和适用阶段。
- 结构 schema：描述结构卡、样例拆解结果、新内容画像、迁移映射和生成方案等核心产物，schema 变化应保留版本信息。
- 评估标准：沉淀对结构拆解、结构迁移、新方案质量、多版本对比的检查标准。

这些资产应可被版本化、引用和追踪。

当前不保留空目录占位；只有当某类资产有实际文件时，才创建对应目录。

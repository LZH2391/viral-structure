## 仓库工作约定

### Git 处理
- 每完成一轮可交付的变更后，及时提交 `commit`。
- 提交前先确认 `git status`，只保留本次任务相关的文件。

### 中间产物
- 中间产物、临时文件、生成结果优先加入 `.gitignore`。
- 已经产生但不应入库的文件，及时清理或补充忽略规则。

### 例外说明
- 只有在任务明确要求时，才提交配置文件、工作区文件或其他本地环境产物。
